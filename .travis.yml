language: ruby
rvm:
- 2.2.3
script:
#- aspell
#- echo "YouTube" | aspell -a
#- printenv
- make bootstrap
- make doit
before_install:
- sudo pip install linkchecker
addons:
  apt:
    packages:
    - aspell
    - aspell-en
    - nginx
    - linkchecker

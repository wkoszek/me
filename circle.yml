machine:
  ruby:
    version:
      2.2.2
  services:
    - docker
test:
  override:
    - echo "test running"
dependencies:
  pre:
    - mkdir /tmp/me
    - docker pull wkoszek/me
    - docker run -ti -v `pwd`:/tmp/me -w /tmp/me wkoszek/me
#    - make bootstrap
#    - make b
#    - bundle exec middleman sitemap_ping
    # In case image_optim_pack doesn't work, one can use this:
    #- sudo apt-get install -y advancecomp gifsicle jhead jpegoptim libjpeg-progs optipng pngcrush pngquant

general:
  branches:
      ignore:
            - gh-pages # list of branches to ignore
